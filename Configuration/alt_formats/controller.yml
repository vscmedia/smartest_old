app:
  modules: Modules/:System/Modules/
  catch-all: false
  domain-path: 

modules:
  default:
    param:
      class: Default
      template: Default
      default-method: getSites
    form-forward: 
      add_page_action:
        param: { module: promo, action: mixmagHome }